% Clear
clear;

% import functions
addpath("functions");

% Read image
image = imread("gallary\face0.jpg");
[height, width, ~] = size(image);

chromatics = zeros(height, width, 2);
skin_threshold = [0.5, 0.3];
for i = 1:height
    for j = 1:width
        pixel = image(i, j, :);
        xy = chromaticity(double(pixel));

        if (xy(1) > skin_t)
    end
end

figure, imshow(image), title("Skin Chromatics");